#include <bits/stdc++.h>

#include <atcoder/modint>
using namespace std;
typedef long long ll;
#define I_MAX 2147483647;
#define LL_MAX 9223372036854775806;
#define rep(i, s, n) for (int i = s; i < (int)(n); i++)
#define repl(i, s, n) for (ll i = s; i < (int)(n); i++)

int main() {
  int n, m;
  cin >> n >> m;

  vector<int[]> v(m);  // いったんこれで書いてみる.

  int c, tmp;
  rep(i, 0, m) {
    cin >> c;
    rep(j, 0, c) {
      cin >> tmp;
      v[i].push
    }
  }

  for (int i = 0; i < (1 << 10); tmp++) {
    bitset<ビット数> s(tmp);
    // (ビット列sに対する処理)
  }
}
