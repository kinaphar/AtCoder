#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, s, n) for (int i = s; i < (int)(n); i++)
#define repl(i, s, n) for (ll i = s; i < (int)(n); i++)

int main() {
  int n, s;
  cin >> n >> s;
  vector<vector<string>> v1(n, vector<string>(s));
  // vector<set> v2(n);

  int a, b;
  rep(i, 0, n) {
    cin >> a >> b;
    // v[i];
  }

  cout << v1[0][0];

  // cout << minsum;
}
